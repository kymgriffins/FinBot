services:
  - type: web
    name: futures-data-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
      - key: SYMBOLS
        value: ES=F,NQ=F,YM=F,6E=F,CL=F,GC=F,SI=F
      - key: TIMEZONE
        value: America/New_York